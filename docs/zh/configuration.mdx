---
title: "配置"
description: "環境變量與服務商設置"
---

## AI 服務商

在 `.env` 中設置 `AI_PROVIDER_FORMAT` 選擇服務商：

| 格式 | 說明 |
|------|------|
| `gemini` | Google Gemini API（默認） |
| `openai` | OpenAI 兼容 API |
| `vertex` | Google Cloud Vertex AI |
| `lazyllm` | 多廠商國產模型路由 |

## Gemini（默認）

```env
AI_PROVIDER_FORMAT=gemini
GOOGLE_API_KEY=your-api-key
GOOGLE_API_BASE=https://generativelanguage.googleapis.com
```

<Warning>
  Gemini API 免費層僅支持文本生成，不支持圖片生成。
</Warning>

## OpenAI 兼容

```env
AI_PROVIDER_FORMAT=openai
OPENAI_API_KEY=your-api-key
OPENAI_API_BASE=https://api.openai.com/v1
```

## Vertex AI

```env
AI_PROVIDER_FORMAT=vertex
VERTEX_PROJECT_ID=your-gcp-project-id
VERTEX_LOCATION=global
GOOGLE_APPLICATION_CREDENTIALS=./gcp-service-account.json
```

<Tip>
  `gemini-3-*` 系列模型需要 `VERTEX_LOCATION=global`。
</Tip>

## LazyLLM（多廠商路由）

將請求路由到不同國產 AI 廠商：

```env
AI_PROVIDER_FORMAT=lazyllm
TEXT_MODEL_SOURCE=deepseek
IMAGE_MODEL_SOURCE=doubao
IMAGE_CAPTION_MODEL_SOURCE=qwen
```

設置對應廠商的 API Key：

```env
DOUBAO_API_KEY=your-key       # 火山引擎/豆包
DEEPSEEK_API_KEY=your-key     # DeepSeek
QWEN_API_KEY=your-key         # 阿裡雲/通義千問
GLM_API_KEY=your-key          # 智譜 GLM
SILICONFLOW_API_KEY=your-key  # 硅基流動
SENSENOVA_API_KEY=your-key    # 商湯日日新
MINIMAX_API_KEY=your-key      # MiniMax
```

## 可編輯 PPTX 導出

配置百度 OCR 以獲得更好的可編輯導出效果（有充足免費額度）：

```env
BAIDU_OCR_API_KEY=your-baidu-ocr-key
```

從[百度智能雲平台](https://console.bce.baidu.com/iam/#/iam/apikey/list)獲取 API Key。

## 運行時設置覆蓋

通過網頁設置頁面配置的参數會存儲在數據庫中，優先級高於 `.env`。點擊"還原默認設置"可恢複為 `.env` 中的值。
